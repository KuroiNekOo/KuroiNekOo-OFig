<h1 class="my-4">O'Fig</h1>       
<h5>Catégories</h5>
<div class="list-group">
  <% for (const category of countFigOfCat) { %>
    <a class="list-group-item d-flex justify-content-between align-items-center" href="/categories/<%= category.name %>">
      Les <%= category.name %>
      <span class="badge badge-primary badge-pill"><%= category.nb_figurine %></span>
    </a>
  <% } %>
</div>